# Backlog

Usa esta carpeta para convertir el PRD en trabajo ejecutable.

Sugerencia inicial:

1. Definir MVP tecnico del `Track A`.
2. Elegir stack de `apps/web` y `services/backend`.
3. Definir el alcance minimo del companion en `apps/mobile`.
4. Desglosar features verificadas vs inferidas.

